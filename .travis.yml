language: php

php:
    - 7.2
    - 7.3
    - 7.4
    - 8.0
    - nightly

jobs:
  allow_failures:
  - php: 7.4
  - php: nightly

before_install:
    - composer self-update

install:
    - travis_retry composer install --no-interaction --prefer-source

script:
    - composer run lint
    - composer run test
